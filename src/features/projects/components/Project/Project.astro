---
import ProjectImage from '@/features/projects/components/ProjectImage/index.astro'
import type { ProjectType } from '../../types/project'
import ProjectLinks from './Links/index.astro'
import styles from './Project.module.css'
import ProjectTechstack from './Techstack/index.astro'

interface Props {
  project: ProjectType
}

const { project } = Astro.props
const { title, descriptionHtml, links, techstack, images } = project
---
<article class={styles.project}>
  <header>
    <h1 class={styles.headerTitle}>{title}</h1>
    <div set:html={descriptionHtml ?? ''}/>
  </header>

  {images.map((image) => (
    <ProjectImage
      image={image}
      alt={title}
      className={styles.fullContainer}
    />
  ))}

  <footer class={styles.meta}>
    {links && <ProjectLinks links={links} />}
    {techstack && <ProjectTechstack techstack={techstack} />}
  </footer>
</article>
